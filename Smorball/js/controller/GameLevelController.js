/**
 * Created by user on 9/12/14.
 */
function GameLevelController(config){
    this.config = config;

    GameLevelController.prototype.init = function(){
        //define stage
        //give background image
    }

    var initLevels = function(){
        var json = [{"level" : 1, "image" : "jpg", "maxPoints": 2000, "points" : 0},
            {"level" : 2, "image" : "jpg", "maxPoints": 2000, "points" : 0}]

        //parse the json
        //var config = {"level" : 1, "image" : "jpg", "maxPoints": 2000, "points" : 0};
        // var gameLevel = new GameLevel(config);
        // add to gameLevel array
        // add to stage

    }

    var showLevel = function(){
        // loop the gamelevel array and set positions
    }



}